# 阶段 5.4 三年级下册纵向扩展审计

处理日期：2026-07-23。版本 A 私有输入扩展至三年级下册；版本 B 仍止于三年级上册。所有教材均不进入公开树。

## 输入与抽样

- 私有输入：`教材/待处理/3下数学-北师大2024版.pdf`，109 页，未加密。
- 已视觉核验：多位数乘一位数（印刷页 3）、通过折叠辨认轴对称（印刷页 21）、周长的边界含义与网格计算（印刷页 34）、两位数除以一位数及余数（印刷页 48）、同加数下比较关系（印刷页 74）、数据整理与表示（印刷页 80、82）。
- 公开输出只保留独立撰写的目标、课标 ID 和最小页码定位；没有保留原文、题目、答案、插图、扫描页或 OCR。

## 结果

- 新增 4 个 `reviewed`、`diagnostic` 候选节点，新增 1 个课程标准能力锚点。
- 对轴对称和数据整理只登记为既有稳定节点的纵向呈现证据，避免用册次重复制造 ID。
- 未新增依赖边；现有 18 条边仍为 `soft`，没有 `hard`、`approved` 或专家确认数据。
- 自动校验结果：56 条课标登记、19 个能力锚点、95 个节点、18 条依赖，图无环；发布树未发现教材、OCR 或其他私有输入。

## 跨版本边界

- 版本 B 尚无三年级下册输入，因此新增节点和等量关系锚点一律保持 `needs-additional-input`。
- 此状态代表输入范围不对称，不是课程冲突、未通过验证或否定版本 A 节点。
- 下一轮横向核验优先级：版本 B 三年级下册，其次为后续册次中可直接定位的同一能力范围。
